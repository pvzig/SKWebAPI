use_frameworks!

target 'SKWebAPI macOS' do
	platform :osx, '10.11'
	pod 'SKCore', '~> 4.1.0'
end

target 'SKWebAPI iOS' do
	platform :ios, '9.0'
	pod 'SKCore', '~> 4.1.0'
end

target 'SKWebAPI tvOS' do
	platform :tvos, '9.0'
	pod 'SKCore', '~> 4.1.0'
end
